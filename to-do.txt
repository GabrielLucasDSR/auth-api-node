Concluído
✅ Estrutura do projeto e servidor no ar; pastas organizadas.
✅ Rotas e controllers conectados; middleware de autenticação validado; rota protegida funcional.
✅ Fluxo completo testado (register → login → access/refresh → rota protegida); API estável.
✅ Service layer isolado; controllers finos e mapeando erros; domínio sem dependência de HTTP.
✅ Logout implementado; testes automatizados (Jest + Supertest) cobrindo e2e e repositórios.
✅ Migração para DB: Postgres via docker-compose; Prisma com schema versionado; repositórios isolam persistência; testes verdes.
✅ Erros de domínio (`AppError`) e mapeamento consistente para HTTP.
✅ Refresh token persistido com jti único, rotação e revogação; validação de estado no banco.
✅ Validação de payloads com Zod; mensagens unificadas (`INVALID_PAYLOAD`).
✅ Logs estruturados (pino) com correlation id por requisição; `req.log` disponível nas rotas.
✅ Lógica de persistência protegida; arquitetura clara e testada ponta a ponta.

Next step:
- CI: GitHub Actions rodando docker-compose + npm test, incluindo migrações.
- Observabilidade extra: logs de eventos de negócio (ex.: logins, 401/403) e métricas básicas.
- Segurança: rate limiting em rotas de auth; bloqueio/expiração de refresh comprometido.
- Validação/UX: uniformizar respostas de erro (payload e domínio) com códigos legíveis e trace-id no corpo.
- Qualidade: mais testes unitários nos services (erros e bordas) e testes de contrato das rotas.
- Manutenção: checklist de envs (dev/test/prod), `LOG_LEVEL` e `DATABASE_URL` documentados no README.
- Performance: opcional cache curto para /users/me e profiling de middleware.
- Deploy-ready: scripts de seed mínimo e instruções de produção (build, migrations, env vars).
